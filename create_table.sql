-- creates a table called first_table
CREATE TABLE IF NOT EXISTS proveedor (id INT AUTO_INCREMENT, name VARCHAR(256) NOT NULL , product_id VARCHAR(256) NOT NULL, quantity INT NOT NULL, quantity_buy INT NOT NULL, PRIMARY KEY (id));
